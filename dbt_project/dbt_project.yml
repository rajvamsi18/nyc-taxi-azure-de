name: 'nyctaxi_dbt'
version: '1.0.0'
config-version: 2

profile: 'nyctaxi_dbt'

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  nyctaxi_dbt:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: view
      +schema: gold
